{
  "framework": "vite",
  "installCommand": "npm install -g pnpm && pnpm --dir src/frontend install --no-frozen-lockfile",
  "buildCommand": "pnpm --dir src/frontend build",
  "outputDirectory": "src/frontend/dist"
}
